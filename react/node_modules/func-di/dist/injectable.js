import { clone, freeze } from "./shared.js";
export const injectable = (token, dependencies, factory, disposer) => freeze({
    type: "di-injectable",
    token,
    dependencies: freeze(clone(dependencies)),
    factory,
    disposer,
});
export const factory = (token, func, disposer) => injectable(token, {}, func, disposer);
//# sourceMappingURL=injectable.js.map