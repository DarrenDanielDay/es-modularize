import { injectable } from "./injectable.js";
import { consumer } from "./consumer.js";
import { clone, freeze } from "./shared.js";
export const inject = (dependencies) => freeze({
    type: "di-injection",
    dependencies: freeze(clone(dependencies)),
    with: (newMapping) => 
    // @ts-expect-error Dynamic Implementation
    inject({
        ...dependencies,
        ...newMapping,
    }),
    for: (factory) => consumer(dependencies, factory),
    implements: (token, factory, disposer) => injectable(token, dependencies, factory, disposer),
});
//# sourceMappingURL=inject.js.map