/**
 * @internal
 */
const identity = (input) => input;
/**
 * @internal
 */
const shallowClone = (input) => ({ ...input });
/**
 * @internal
 */
export const freeze = process.env.NODE_ENV === "production" ? identity : Object.freeze;
/**
 * @internal
 */
export const clone = process.env.NODE_ENV === "production" ? identity : shallowClone;
//# sourceMappingURL=shared.js.map