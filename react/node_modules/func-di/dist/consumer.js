import { clone, freeze } from "./shared.js";
import { __FUNC_DI_CONTAINER__ } from "./token.js";
/**
 * Create a consumer with dependencies.
 * @param dependencies the dependencies
 * @param factory the instance factory function
 * @returns Consumer
 */
export const consumer = (dependencies, factory) => freeze({
    type: "di-consumer",
    dependencies: freeze(clone(dependencies)),
    factory,
});
export const dynamicConsumer = (factory) => consumer({ c: __FUNC_DI_CONTAINER__ }, ({ c }) => factory(c));
//# sourceMappingURL=consumer.js.map